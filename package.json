{
  "devDependencies": {
    "@vuepress/plugin-google-analytics": "^1.9.5",
    "@vuepress/theme-blog": "^2.3.3",
    "vuepress": "^1.9.5"
  },
  "scripts": {
    "dev": "NODE_OPTIONS='--openssl-legacy-provider' vuepress dev blog",
    "build": "NODE_OPTIONS='--openssl-legacy-provider' vuepress build blog"
  }
}
